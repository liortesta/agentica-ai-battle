version: '3.8'

services:
  agentica-battle:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./server/.env:/app/.env:ro